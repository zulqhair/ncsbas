@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between mb-4"><div><h1 class="h2">My Assessments</h1><p class="text-secondary">Answer the NCSB questions directly in the system.</p></div><form method="POST" action="{{ route('assessments.create') }}">@csrf<button class="btn btn-primary">New Assessment</button></form></div><div class="card"><div class="card-body">@forelse($assessments as $assessment)<a class="d-block border-bottom py-2" href="{{ route('assessments.show',$assessment) }}">Assessment #{{ $assessment->id }} <span class="text-secondary">{{ ucfirst($assessment->status) }}</span></a>@empty<p class="mb-0 text-secondary">No assessments yet.</p>@endforelse</div></div>@endsection
