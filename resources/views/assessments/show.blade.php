@extends('layouts.app')
@section('content')<h1 class="h2">Assessment #{{ $assessment->id }}</h1><p class="text-secondary">Choose Yes or No for each question. You can save and continue later.</p><form method="POST" action="{{ route('assessments.save',$assessment) }}">@csrf @method('PUT')@foreach($elements as $number=>$questions)<div class="card mb-4"><div class="card-header"><strong>{{ $number }}. {{ $questions->first()->element_name }}</strong></div><div class="card-body">@foreach($questions as $q)<div class="border-bottom pb-3 mb-3"><p>{{ $q->number }}. {{ $q->question }}</p><label class="me-3"><input type="radio" name="answers[{{ $q->id }}]" value="Yes" @checked(($answers[$q->id]??null)==='Yes')> Yes</label><label><input type="radio" name="answers[{{ $q->id }}]" value="No" @checked(($answers[$q->id]??null)==='No')> No</label></div>@endforeach</div></div>@endforeach<button class="btn btn-primary">Save Draft</button></form>@endsection
